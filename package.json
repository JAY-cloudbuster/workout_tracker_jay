{
  "name": "gymtracker-pro",
  "version": "1.0.0",
  "description": "Intelligent evidence-based workout tracking SaaS",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "install:prod": "npm install --include=dev && cd server && npm install --include=dev && cd ../client && npm install --include=dev",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "build:server": "cd server && npm install --include=dev && npm run build",
    "build:client": "cd client && npm run build",
    "build": "npm run build:server",
    "start": "cd server && npm start",
    "seed": "cd server && npm run seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
